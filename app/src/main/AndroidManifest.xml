<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arist.proyectosmartfashion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu2Activity" />
        <activity
            android:name=".BaseAvtivity"
            android:label="@string/title_activity_base_avtivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EsmalteActivity"
            android:label="@string/title_activity_esmalte"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RuborActivity"
            android:label="@string/title_activity_rubor"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SombraActivity"
            android:label="@string/title_activity_sombra"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".lipsActivity"
            android:label="@string/title_activity_lips"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RimelActivity"
            android:label="@string/title_activity_rimel"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DeliActivity"
            android:label="@string/title_activity_deli"
            android:theme="@style/AppTheme" />
        <activity android:name=".inicioActivity"></activity>
    </application>

</manifest>